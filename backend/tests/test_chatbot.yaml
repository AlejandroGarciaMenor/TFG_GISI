config:
  target: "https://tfg-app.xyz"
  phases:
    - duration: 30
      arrivalRate: 2
  processor: "./test_perfil.js"
scenarios:
  - beforeScenario: "beforeScenario"
    flow:
      - post:
          url: "/chatbot/chatbot-conversacion"
          headers:
            Authorization: "Bearer {{ jwt }}"
          json:
            input: "Hola, me siento ansioso y no se qué hacer"
            user_id: {{ userId }}
          expect:
            - statusCode: 200